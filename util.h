#ifndef UTIL_H
#define UTIL_H
#include "heap.h"
using namespace std;

//parse keyboard commands
int nextCommand(int *s, int *t, int *i);

// read file
void readFile(Graph* graph);

#endif
